out vec4 color;
layout(binding=0) uniform sampler2DArray tex;
void main(){
    vec2 texc = gl_PointCoord;
    color = texture( tex, vec3(texc,0.0));
    color.a *= max( 1.0 - psElapsed * 1.0 / psMaxLife, 0.0);
}
